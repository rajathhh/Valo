import SearchCard from "@/components/bus/search-card";

export default function BusPage() {
  return (
    <main className="min-h-screen bg-slate-100 flex items-center justify-center p-8">
      <SearchCard />
    </main>
  );
}